[workspace]
name = "opkit-minimal"
description = "Smallest environment an op can run in: python, numpy, appose."
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64", "win-64"]
version = "0.1.0"

[dependencies]
python = "3.11.*"
numpy = "*"
appose = ">=0.11"
